Stage 1 : FRCNN Damage + Part Detection
Stage 2 : GPT-4o Repair Plan Generation  (openai_utils)
Stage 3 : Visual Repair Graph (render_visual_guidance)
Stage 4 : PyBullet Execution (robot_executor.py  → next step)


FRCNN detection → damaged part ("front_left_leg" broken)
↓
Look up dependencies from chair_graph.py
↓
Build hierarchical JSON (root → seat → legs)
↓
Pass graph + damage context to GPT-4o → structured repair plan
↓
Use robot_executor.py to simulate the repair sequence


Manual2Skill's "Assembly Graph" → becomes "Repair Graph" for Repair2Skill


 ┌───────────────────────────────┐
 │   Image Capture / Upload      │
 │ (Raspberry Pi Camera / File)  │
 └──────────────┬────────────────┘
                │
                ▼
 ┌───────────────────────────────┐
 │  Stage 1: Damage Detection    │
 │   (Faster R-CNN Model)        │
 │ → Outputs: stage1_parts.json  │
 └──────────────┬────────────────┘
                │
                ▼
 ┌───────────────────────────────┐
 │  Stage 2: GPT-4o Repair Plan  │
 │   Structured repair sequence  │
 │ → Outputs: repair_plan_*.json │
 └──────────────┬────────────────┘
                │
                ▼
 ┌───────────────────────────────┐
 │ Stage 3: Repair Graph Builder │
 │   (Manual2Skill-style graph)  │
 │ → Outputs: repair_graph_*.json│
 └──────────────┬────────────────┘
                │
                ▼
 ┌───────────────────────────────┐
 │ Stage 4: Visual Repair Guide  │
 │  Highlight damaged components │
 │ → Outputs: visual_guides/*.png│
 └──────────────┬────────────────┘
                │
                ▼
 ┌───────────────────────────────┐
 │ Stage 5: Robotic Execution    │
 │  (PyBullet Simulation)        │
 │  Executes repair plan & graph │
 └───────────────────────────────┘
